// This file is included to complete the "vanilla stack" (HTML, CSS, JS).

document.addEventListener('DOMContentLoaded', () => {
    // Console message to show the script is running
    console.log("Mike Matas Portfolio Replication Loaded.");

    // You can add interactive features here later, such as:
    
    // 1. Implementing a subtle hover effect on the project items (using JS instead of only CSS)
    /*
    const items = document.querySelectorAll('.project-item');
    items.forEach(item => {
        item.addEventListener('mouseenter', () => {
            item.style.backgroundColor = '#f0f0f0';
        });
        item.addEventListener('mouseleave', () => {
            item.style.backgroundColor = 'transparent';
        });
    });
    */

    // 2. Making the "0" counter actually increment on click, if desired.
    
});